import React from "react";
import {Button, StyleSheet, TextInput} from "react-native";

const Bouton = props => {
    return (
        <Button {...props} color="#1A1D53" onPress={()=>{}} />
    );
};

export default Bouton;
